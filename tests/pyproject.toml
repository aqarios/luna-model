[project]
name = "tests"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "accext",
  "luna_model",
]

[tool.uv.sources]
accext = { path = "../extensions/example-extension" }
luna_model = { path = "../py-lunamodel" }
